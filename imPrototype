lib/bundler/plugin.rb:

> @@ -197,7 +197,16 @@ def load_plugin(name)
>        # done to avoid conflicts
>        path = index.plugin_path(name)
>  
> +      if insert_index = Bundler.rubygems.load_path_insert_index
https://github.com/asutoshpalai
